package editor.utiles;

import java.awt.Cursor;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.JFrame;
import javax.swing.JOptionPane;
import editor.model.*;
public class TestActionListener implements ActionListener { // обработчик событий.
	public void actionPerformed(ActionEvent e) {
		// Код, который нужно выполнить при нажатии
		/*
		 * String message = "\"Это уведмление\"\n" + "Программа-таки реагирует\n" +
		 * "на нажатие кнопки"; JOptionPane.showMessageDialog(new JFrame(), message,
		 * "Dialog", JOptionPane.ERROR_MESSAGE); }
		 */
		//Button.paneFile.setCursor(Cursor.getPredefinedCursor(Cursor.HAND_CURSOR));
	}
}
